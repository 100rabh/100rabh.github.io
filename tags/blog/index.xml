<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Blog on आरम्भ</title>
    <link>http://100rabh.github.io/tags/blog/</link>
    <description>Recent content in Blog on आरम्भ</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <lastBuildDate>Mon, 02 Feb 2015 13:07:31 +0200</lastBuildDate>
    <atom:link href="http://100rabh.github.io/tags/blog/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>How to host a static website/blog with Hugo, Github Pages and Wercker</title>
      <link>http://100rabh.github.io/2015/02/02/how-to-host-a-static-website/blog-with-hugo-github-pages-and-wercker</link>
      <pubDate>Mon, 02 Feb 2015 13:07:31 +0200</pubDate>
      
      <guid>http://100rabh.github.io/2015/02/02/how-to-host-a-static-website/blog-with-hugo-github-pages-and-wercker</guid>
      <description>

&lt;h2 id=&#34;what-s-up-with-hugo:964c172ef5e837f03573891408d8d631&#34;&gt;What&amp;rsquo;s up with &lt;code&gt;HUGO&lt;/code&gt;&lt;/h2&gt;

&lt;p&gt;Hugo is a static site generator. Sites generated with Hugo tends to be &lt;a href=&#34;https://youtu.be/CdiDYZ51a2o&#34;&gt;fast&lt;/a&gt;, easy to host on different providers like AWS, GitHub Pages, Google Cloud etc. Visit official Hugo wesite for &lt;a href=&#34;https://gohugo.io/overview/introduction/&#34;&gt;more&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&#34;create-a-static-site-with-hugo:964c172ef5e837f03573891408d8d631&#34;&gt;Create a static site with Hugo&lt;/h2&gt;

&lt;p&gt;First of all you need to have hugo installed in your working Env. Basically you may want to refer &lt;a href=&#34;https://gohugo.io/overview/installing/&#34;&gt;Hugo Installation documentation&lt;/a&gt; for details. Which is pretty much straightforward.
After that follow these commands to create site with hugo:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  $ hugo new site path/to/site
  $ cd path/to/site
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The new site will have the following structure:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  ▸ archetypes/
  ▸ content/
  ▸ data/
  ▸ layouts/
  ▸ static/
  config.toml
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now you have static site without any theme. If you want to have one you are in the right place Hugo has rich &lt;a href=&#34;https://gohugo.io/themes/overview/&#34;&gt;theme&lt;/a&gt; support.&lt;/p&gt;

&lt;h2 id=&#34;run-your-site-with-hugo-server:964c172ef5e837f03573891408d8d631&#34;&gt;Run your site with Hugo server&lt;/h2&gt;

&lt;pre&gt;&lt;code&gt;   $ hugo server
   OR If on windows use:
   $ hugo server --renderToDisk
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;On console you will see a message like: &lt;code&gt;Web Server is available at http://localhost:1313&lt;/code&gt;.
Go to the browser and verify the site.
You can have many &lt;a href=&#34;http://gohugo.io/overview/quickstart/#step-7-have-fun&#34;&gt;options&lt;/a&gt;, with hugo to build the site as you want. Just play around with those options untill you have the site you want.&lt;/p&gt;

&lt;h2 id=&#34;setup-git:964c172ef5e837f03573891408d8d631&#34;&gt;Setup &amp;lsquo;git&amp;rsquo;&lt;/h2&gt;

&lt;p&gt;Now that your site been setup locally as desired. You need to host it &lt;code&gt;live&lt;/code&gt;. We will use &lt;a href=&#34;https://pages.github.com/&#34;&gt;github pages&lt;/a&gt; for that.
There are two option we have here to host with github pages:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  1:  User or Organization site (served from `master` branch)
  2:  Project site (served from `gh-pages` branch)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We are going with option 1 here.
So just follow the inst. in github pages i.e. setup repo with name &lt;username&gt;.github.io.&lt;/p&gt;

&lt;p&gt;Here username should be your/org&amp;rsquo;s github username.
Create two branches one master which will have the public folder contents (from the site generated with hugo).
Other you can name anything and this will actually hold the source contents which will be used by hugo to create site for.&lt;/p&gt;

&lt;p&gt;Now if you have read the hugo docs(which you should), you know that there will be a /public folder created by hugo and this contains actual contents of your site which will be hosted by github (if placed in master branch of repo). But since We are
creating site with hugo we don&amp;rsquo;t  nedd to commit this. So use .gitignore file to ignore /public folder.&lt;/p&gt;

&lt;p&gt;So at last we have a repo in git with two branches one master containing &lt;code&gt;/public&lt;/code&gt; folder contents and other branch(suppose source) containing source contents of hugo site.
If all setup good, you will be able to see your site at http://&lt;username&gt;.github.io/
Now we just have to automated the things so that we don&amp;rsquo;t have to commit to the both branch each time we made a change to our site.&lt;/p&gt;

&lt;h2 id=&#34;automation-with-wercker:964c172ef5e837f03573891408d8d631&#34;&gt;Automation with &lt;code&gt;wercker&lt;/code&gt;&lt;/h2&gt;

&lt;p&gt;Up to now you have a site hosted from github pages.
Repo on github with branches master and source.
To make any change in site you will be commiting changes to source branch (just to maintain your hugo source code).
and changes of /public folder to master branch.&lt;/p&gt;

&lt;p&gt;Now we will automate this process using wercker.
But before that just push wercker.yml file with following code in &lt;code&gt;source&lt;/code&gt; branch.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  box: debian
  build:
    steps:
      - arjen/hugo-build:
          version: &amp;quot;0.15&amp;quot;
          flags: --buildDrafts=true

  deploy:
    steps:
      - install-packages:
          packages: git ssh-client
      - leipert/git-push:
          gh_oauth: $GIT_TOKEN
          basedir: public
          clean_removed_files: true
          branch: master
          repo: &amp;lt;Your github repo&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;No go to &lt;a href=&#34;http://wercker.com&#34;&gt;wercker&lt;/a&gt; and follow steps:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  1: Create wercker account using github or else.
  2: Select github repo (in this case &amp;lt;username&amp;gt;.github.io) 
  3: Configure access (choose recommended)
  4: Setup your wercker.yml (will be done automatically as we already have it in our repo branch)
  5: You can OR can&#39;t make this wercker public
  6: Build it it should be GREEN
  7: Go to settings and for option `Ignored branches` add `master` branch.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now since wercker build step is GREEN, We will try to make it deploy to master branch:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  1: Got to newly build wercker application -&amp;gt; settings (right top)
  2: Add deploy target (choose custom deploy)
  3: Git deploy target a nice name (offcource anything)
  4: Select auto deploy and add branch name `source` (where your source code it in git repo)
  5: Add a new variable `GIT_TOKEN` (as specified in wercker.yml file)
  6: For the text value of `GIT_TOKEN`, You need to goto your github profile setting page.
  7: Select Personal Access Tocken there and generate one (name it anything and copy the value)
  8: Now goto wercker and paste it as `GIT_TOKEN` text value.
  9: Save everything
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now got to make some change to your local hugo site/repo and push those changes to source branch.
You will see your wercker building your changes from source and pushing them to master so that you can see them LIVE !!&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>